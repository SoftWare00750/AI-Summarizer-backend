services:
  - type: web
    name: pagemind-backend
    env: docker # Changed from 'node' to 'docker'
    region: oregon
    plan: free
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000
    healthCheckPath: /health
